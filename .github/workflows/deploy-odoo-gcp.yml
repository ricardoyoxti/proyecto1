# .github/workflows/deploy-odoo-gcp.yml
name: 🚀 Deploy Odoo 18 to Google Cloud

on:
  workflow_dispatch:
    inputs:
      instance_name:
        description: 'Nombre base de la instancia (se añadirá timestamp único)'
        required: false
        default: 'odoo'
      machine_type:
        description: 'Tipo de máquina'
        required: true
        default: 'e2-standard-2'
        type: choice
        options:
        - 'e2-micro'
        - 'e2-small'
        - 'e2-medium'
        - 'e2-standard-2'
        - 'e2-standard-4'
      zone:
        description: 'Zona de GCP'
        required: true
        default: 'southamerica-west1-a'
      disk_size:
        description: 'Tamaño del disco (GB)'
        required: true
        default: '20'

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔐 Authenticate to Google Cloud
      uses: google-github-actions/auth@v1
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: ☁️ Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v1

    - name: 🏷️ Generate instance name
      id: generate_name
      run: |
        BASE_NAME="${{ github.event.inputs.instance_name }}"
        TIMESTAMP=$(date +%Y%m%d-%H%M%S)
        INSTANCE_NAME="${BASE_NAME}-${TIMESTAMP}"
        
        # Validar nombre de instancia (solo letras minúsculas, números y guiones)
        if [[ ! $INSTANCE_NAME =~ ^[a-z0-9-]+$ ]]; then
          echo "❌ Error: El nombre de la instancia solo puede contener letras minúsculas, números y guiones"
          exit 1
        fi
        
        echo "INSTANCE_NAME=$INSTANCE_NAME" >> $GITHUB_OUTPUT
        echo "BASE_NAME=$BASE_NAME" >> $GITHUB_OUTPUT
        echo "🏗️ Nombre de instancia generado: $INSTANCE_NAME"

    - name: 🔍 Verify startup script exists
      run: |
        if [ ! -f "startup-script.sh" ]; then
          echo "❌ Error: startup-script.sh no encontrado en el directorio raíz"
          exit 1
        fi
        echo "✅ startup-script.sh encontrado"
        echo "📁 Directorio actual: $(pwd)"
        echo "📋 Contenido del directorio:"
        ls -la
        echo "📄 Primeras líneas del startup-script.sh:"
        head -5 startup-script.sh

    - name: 🖥️ Create VM Instance
      run: |
        INSTANCE_NAME="${{ steps.generate_name.outputs.INSTANCE_NAME }}"
        
        echo "🏗️ Creando instancia: $INSTANCE_NAME"
        echo "📁 Directorio de trabajo: $(pwd)"
        echo "📄 Verificando startup-script.sh: $(ls -la startup-script.sh)"
        
        gcloud compute instances create $INSTANCE_NAME \
          --project=${{ env.PROJECT_ID }} \
          --zone=${{ github.event.inputs.zone }} \
          --machine-type=${{ github.event.inputs.machine_type }} \
          --network-interface=network-tier=PREMIUM,stack-type=IPV4_ONLY,subnet=default \
          --maintenance-policy=MIGRATE \
          --provisioning-model=STANDARD \
          --service-account=${{ secrets.GCP_SERVICE_ACCOUNT_EMAIL }} \
          --scopes=https://www.googleapis.com/auth/devstorage.read_only,https://www.googleapis.com/auth/logging.write,https://www.googleapis.com/auth/monitoring.write,https://www.googleapis.com/auth/servicecontrol,https://www.googleapis.com/auth/service.management.readonly,https://www.googleapis.com/auth/trace.append \
          --tags=odoo-server,http-server,https-server \
          --image-family=ubuntu-2204-lts \
          --image-project=ubuntu-os-cloud \
          --boot-disk-size=${{ github.event.inputs.disk_size }} \
          --boot-disk-type=pd-balanced \
          --boot-disk-device-name=$INSTANCE_NAME \
          --no-shielded-secure-boot \
          --shielded-vtpm \
          --shielded-integrity-monitoring \
          --labels=environment=production,application=odoo,created-by=github-actions \
          --reservation-affinity=any \
          --metadata-from-file startup-script=startup-script.sh \
          --metadata instance-name=$INSTANCE_NAME,deployment-time=$(date -u +"%Y-%m-%dT%H:%M:%SZ"),github-actor=${{ github.actor }}

    - name: 🔥 Create firewall rules
      run: |
        # Regla para Odoo (puerto 8069)
        gcloud compute firewall-rules create allow-odoo-${{ github.run_number }} \
          --project=${{ env.PROJECT_ID }} \
          --direction=INGRESS \
          --priority=1000 \
          --network=default \
          --action=ALLOW \
          --rules=tcp:8069 \
          --source-ranges=0.0.0.0/0 \
          --target-tags=odoo-server \
          --description="Allow Odoo access on port 8069" || \
        gcloud compute firewall-rules create allow-odoo \
          --project=${{ env.PROJECT_ID }} \
          --direction=INGRESS \
          --priority=1000 \
          --network=default \
          --action=ALLOW \
          --rules=tcp:8069 \
          --source-ranges=0.0.0.0/0 \
          --target-tags=odoo-server \
          --description="Allow Odoo access on port 8069" || echo "Firewall rule already exists"
        
        # Regla para SSH (si no existe)
        gcloud compute firewall-rules create allow-ssh \
          --project=${{ env.PROJECT_ID }} \
          --direction=INGRESS \
          --priority=1000 \
          --network=default \
          --action=ALLOW \
          --rules=tcp:22 \
          --source-ranges=0.0.0.0/0 \
          --target-tags=odoo-server \
          --description="Allow SSH access" || echo "SSH firewall rule already exists"

    - name: ⏳ Wait for startup script to complete
      run: |
        INSTANCE_NAME="${{ steps.generate_name.outputs.INSTANCE_NAME }}"
        echo "⏳ Esperando a que complete el startup script..."
        
        # Esperar hasta 15 minutos para que complete la instalación
        for i in {1..30}; do
          echo "🔍 Verificando progreso... (intento $i/30)"
          
          # Verificar si Odoo está respondiendo
          EXTERNAL_IP=$(gcloud compute instances describe $INSTANCE_NAME --zone=${{ github.event.inputs.zone }} --format="get(networkInterfaces[0].accessConfigs[0].natIP)")
          
          if curl -s --connect-timeout 5 http://$EXTERNAL_IP:8069 > /dev/null 2>&1; then
            echo "✅ Odoo está respondiendo!"
            break
          fi
          
          if [ $i -eq 30 ]; then
            echo "⚠️ Timeout esperando a Odoo. Verificar logs manualmente."
            echo "🔧 Comando para ver logs: gcloud compute ssh $INSTANCE_NAME --zone=${{ github.event.inputs.zone }} --command='sudo tail -f /var/log/startup-script.log'"
          else
            echo "⏳ Odoo aún no está listo. Esperando 30 segundos..."
            sleep 30
          fi
        done

    - name: 📊 Get instance details and debugging info
      run: |
        INSTANCE_NAME="${{ steps.generate_name.outputs.INSTANCE_NAME }}"
        echo "🎉 Instancia creada exitosamente!"
        echo "📍 Detalles de la instancia:"
        gcloud compute instances describe $INSTANCE_NAME \
          --zone=${{ github.event.inputs.zone }} \
          --format="table(name,status,machineType.basename(),scheduling.preemptible.yesno(yes='Yes',no='No'):label=PREEMPTIBLE,networkInterfaces[0].accessConfigs[0].natIP:label=EXTERNAL_IP,disks[0].diskSizeGb)"
        
        echo ""
        echo "🌐 IP Externa:"
        EXTERNAL_IP=$(gcloud compute instances describe $INSTANCE_NAME --zone=${{ github.event.inputs.zone }} --format="get(networkInterfaces[0].accessConfigs[0].natIP)")
        echo "IP: $EXTERNAL_IP"
        
        echo ""
        echo "🏷️ Información del despliegue:"
        echo "Nombre generado: $INSTANCE_NAME"
        echo "Nombre base: ${{ steps.generate_name.outputs.BASE_NAME }}"
        echo "Desplegado por: ${{ github.actor }}"
        echo "Fecha: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
        
        echo ""
        echo "🔗 URLs de acceso:"
        echo "Odoo: http://$EXTERNAL_IP:8069"
        echo "SSH: gcloud compute ssh $INSTANCE_NAME --zone=${{ github.event.inputs.zone }}"
        
        echo ""
        echo "📋 Credenciales por defecto:"
        echo "  - Database: odoo"
        echo "  - User: admin"
        echo "  - Password: admin"
        
        echo ""
        echo "🔧 Comandos de debugging:"
        echo "Ver logs startup: gcloud compute ssh $INSTANCE_NAME --zone=${{ github.event.inputs.zone }} --command='sudo tail -f /var/log/startup-script.log'"
        echo "Ver logs Odoo: gcloud compute ssh $INSTANCE_NAME --zone=${{ github.event.inputs.zone }} --command='sudo tail -f /var/log/odoo/odoo.log'"
        echo "Estado servicio: gcloud compute ssh $INSTANCE_NAME --zone=${{ github.event.inputs.zone }} --command='sudo systemctl status odoo'"
        
        echo ""
        echo "🗑️ Para eliminar instancia y firewall:"
        echo "gcloud compute instances delete $INSTANCE_NAME --zone=${{ github.event.inputs.zone }}"
        echo "gcloud compute firewall-rules delete allow-odoo-${{ github.run_number }} --quiet"
